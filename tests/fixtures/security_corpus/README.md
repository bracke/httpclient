# Security corpus

These deterministic fixtures are safe synthetic inputs for offline parser and boundary tests. They do not include production credentials, live tokens, private keys, or user-specific data.

When adding minimized cases from fuzzing, random fuzz campaigns must print the seed and the failing input should be reduced before being committed here.
